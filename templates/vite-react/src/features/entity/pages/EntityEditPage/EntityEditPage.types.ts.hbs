import type { Update{{entity.name}}Dto } from '@/types/{{entity.slug}}';

export interface {{entity.name}}EditPageState {
  formData: Update{{entity.name}}Dto;
  errors: Record<string, string>;
  initialized: boolean;
}
